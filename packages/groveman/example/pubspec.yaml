name: groveman_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  groveman:
    path: ../

dev_dependencies:
  lint: ^2.8.0

flutter:
  uses-material-design: true
