name: groveman_sentry_example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  groveman: ^1.2.3
  groveman_sentry:
    path: ../
  sentry_flutter: ^9.0.0

dev_dependencies:
  flutter_lints: ^6.0.0

dependency_overrides:
  win32: ^5.14.0

flutter:
  uses-material-design: true
