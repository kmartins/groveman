# groveman_sentry_example

Demonstrates how to use the groveman sentry tree.

## Getting Started

Create a new project in [Sentry](https://sentry.io/) and define `SENTRY_DSN` via Dart environment variable (`--dart-define`).
