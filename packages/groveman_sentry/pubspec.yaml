name: groveman_sentry
description: Groveman tree that sends your logs to Sentry.
version: 1.0.0

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  groveman: ^1.1.0
  meta: ^1.7.0
  sentry: ^6.2.2

dev_dependencies:
  lint: ^1.8.1
  test: ^1.20.1
